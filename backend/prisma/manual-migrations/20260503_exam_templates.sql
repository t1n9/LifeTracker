-- exam_templates / exam_phase_templates / exam_week_plan_templates
-- These tables were created and later removed in the same release cycle.
-- Drop them here so the next file (20260504_drop_exam_templates.sql) becomes a no-op.
DROP TABLE IF EXISTS exam_week_plan_templates;
DROP TABLE IF EXISTS exam_phase_templates;
DROP TABLE IF EXISTS exam_templates;
